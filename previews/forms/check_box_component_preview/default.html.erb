<%= form_with model: model do |form| %>
  <div class="flex items-center gap-2">
    <%= form.check_box :attribute, {checked: checked, disabled: disabled} %>
    <%= form.label :attribute, label, variant: :inline %>
  </div>
<% end %>
