<%= form_with model: model do |form| %>
  <div class="flex items-center gap-2">
    <%= form.radio_button :attribute, :value, checked: checked, disabled: disabled %>
    <%= form.label :attribute, label, value: :value, variant: :inline %>
  </div>
<% end %>
