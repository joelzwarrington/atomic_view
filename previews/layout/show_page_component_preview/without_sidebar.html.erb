<%= render(AtomicView::Components::ShowPageComponent.new) do |page| %>
  <% page.with_title { "Riverside RV Park" } %>
  <% page.with_breadcrumbs { link_to "Parks", "#", class: "text-xs font-semibold text-primary" } %>

  <%= tag.section do %>
    <h2 class="mb-2 text-lg font-semibold text-foreground">Overview</h2>
    <%= render(AtomicView::Components::CardComponent.new) do %>
      582 sites across 3 locations.
    <% end %>
  <% end %>
<% end %>
