<%= tag.div(**html_options, class: html_class, style: "width: #{cell_width}px") do %>
  <span><%= primary_label %></span>
  <span class="text-[10px] font-medium uppercase tracking-wide text-muted-foreground/80"><%= secondary_label %></span>
<% end %>
